class Calculator {
    static add(number1, number2) {
        return number1 + number2;
    }

    static orderNumbers(array) {
        return array;
    }

    static objectChecker(object) {
        return object;
    }
}

module.exports = Calculator;
